services:
  souvenir-bot:
    container_name: souvenir-bot
    working_dir: /SouvenirPerfumeTGBot
    image: yakiza/souvenir-bot:latest
    restart: always
    build:
        context: .
        dockerfile: Dockerfile
    command: sh -c "cd bot && python main.py"
    volumes:
      - souvenir-bot:/SouvenirPerfumeTGBot/data

volumes:
  souvenir-bot:
    name: souvenir-bot
